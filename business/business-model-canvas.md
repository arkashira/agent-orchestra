Generated `business-model-canvas.md` for **agent-orchestra** — full 9-block canvas, 3–5 bullets each, USD + THB (≈36.5).

Key strategic bets baked in:
- **Beachhead:** solo AI-native devs running 2–6 local CLI agents (the tmux-chaos pain), expanding to small teams.
- **Wedge VP:** "no billing ceiling" — you charge for the tool, not tokens/seats — directly counters the billing-limitation pain in the hypothesis.
- **Monetization ladder:** free OSS core → Pro $12/mo (438 THB) → Team $25/seat (913 THB) → self-hosted Enterprise $3k–$12k/yr (109.5k–438k THB), ~80/20 self-serve/enterprise in Y1.
- **Moat:** breadth + freshness of agent adapters (Claude Code, Aider, Codex, Cursor) and the OSS community; explicitly flagged the **maintenance tax** (15–20% of dev time chasing upstream breaking changes) as the structural cost risk.
- **Margin:** local-first → near-zero marginal cost, >90% gross margin; main cost is the lean 2–3 person eng team.

Note: the file at that path previously held a canvas for a different product (`estate-mapper`) — I overwrote it with the agent-orchestra canvas as the task specified. Flag if that prior file needed preserving.